
<main class="main-section">
    <nav>
        <h2>Kwiveti</h2>
        <ul>
            <li><a href="">About</a></li>
            <li><a href="">FAQ</a></li>
            <li><a href="">Contact</a></li>
        </ul>
    </nav>
    <div class="hero-flex">
        <img src="/assets/vault_robot.png" alt="Vault Robot">
        <div class="hero-content">
            <h3>Deal with People anywhere without losing money.</h3>
            <p>Kwiveti provide secure escrow solution that protects every party from losing money during anonymous transactions </p>
            <button>JOIN WAITLIST</button>
        </div>
    </div>
</main>
<style>
	.main-section {
		background-image: linear-gradient(#EECDA3, #EF629F);
        width: 100vw;
        max-width: 100vw;
        min-height: max-content;
        padding: 0 20px 40px;
        display: flex;
        flex-direction: column;
        align-items: center;
	}
    nav {
        width: 100%;
        height: 60px;
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    nav > h2 {

    }
    nav > ul {
        /* display: none; */
        display: flex;
        list-style: none;
    }
    nav > ul > li{
        margin-left: 12px;
    }
    nav > ul > li > a{
        color: #000000;
        text-decoration: none;
        font-weight: 500;
        cursor: pointer;
    }
    .hero-flex{
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    img{
        width: 240px;
    }
    .hero-content{
        /* border: 3px solid black; */
        width: 80%;
        display: flex;
        flex-direction: column;
        align-items: center;
    }
    .hero-content > h3{
        font-size: 24px;
        font-weight: 600;
        margin-bottom: 18px;
        text-align: center;
    }
    .hero-content > p{
        font-size: 16px;
        margin-bottom: 16px;
        text-align: center;
    }
    .hero-content > button{
        background-color: #EECDA3;
        height: 60px;
        margin-top: 24px;
        padding: 0 24px;
        border-radius: 16px;
        border:none;
        width: max-content;
        font-weight: 600;
    }
    @media screen and (min-width: 599px) {
        .main-section {
          justify-content: center;
        }
        nav {
          width: 80%;
          max-width: 1000px;
        }
        nav > ul > li{
          margin-left: 18px;
        }
        nav > ul > li > a{
            font-size: 18px;
        }
        .hero-flex{
            width: 80%;
            max-width: 1000px;
            flex-direction: row-reverse;
            justify-content: space-between;
            margin-top: 36px;
        }
        img{
            width: 40%;
        }
        .hero-content{
            width: 60%;
            max-width: 500px;
            align-items: flex-start;
        }
        .hero-content > h3{
            font-size: 36px;
            margin-bottom: 24px;
            text-align: start;
        }
        .hero-content > p{
            font-size: 24px;
            text-align: start;
        }
    }
</style>
